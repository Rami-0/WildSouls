let $burgerMenu = document.querySelector("#burger-menu");let $navbar = document.querySelector(".navbar");let $cross = document.querySelector(".navbar img");let $body = document.querySelector("body");$burgerMenu.addEventListener("click", function () {	$navbar.style.transform = "translate(0)";	$body.style.overflow = "hidden";});$cross.addEventListener("click", function () {	$navbar.style.transform = "translate(-100%)";	$body.style.overflow = "auto";});let $productListDivs = document.querySelectorAll(	".products-review .products .products-list > div");$productListDivs.forEach((el) => {	el.addEventListener("mouseover", function () {		el.style.color = "white";		el.firstElementChild.style.transform = "translate(10%)";		el.lastElementChild.style.width = "100%";		el.lastElementChild.style.alignSelf = "flex-start";	});	el.addEventListener("mouseout", function () {		el.style.color = "black";		el.firstElementChild.style.transform = "translate(0)";		el.lastElementChild.style.width = "0";		el.lastElementChild.style.alignSelf = "flex-end";	});});let $sliderSlides = document.querySelectorAll(".slider > div");let $sliderButtons = document.querySelectorAll(".slider > .dots > div");let switcherNum = 1;function switcher() {	if (switcherNum == 1) {		$sliderSlides[0].style.opacity = "0";		$sliderButtons[0].classList.remove("checked");		$sliderSlides[1].style.opacity = "1";		$sliderButtons[1].classList.add("checked");		switcherNum = 2;		return;	}	if (switcherNum == 2) {		$sliderSlides[0].style.opacity = "1";		$sliderButtons[0].classList.add("checked");		$sliderSlides[1].style.opacity = "0";		$sliderButtons[1].classList.remove("checked");		switcherNum = 1;	}}let interval = setInterval(switcher, 5000);$sliderButtons.forEach((el, index) => {	el.addEventListener("click", function () {		if (el.classList == "checked") {			clearInterval(interval);			interval = setInterval(switcher, 5000);		} else {			clearInterval(interval);			$sliderSlides[index].style.opacity = "1";			$sliderButtons[index].classList.add("checked");			$sliderSlides[index == 0 ? 1 : 0].style.opacity = "0";			$sliderButtons[index == 0 ? 1 : 0].classList.remove("checked");			interval = setInterval(switcher, 5000);		}	});});